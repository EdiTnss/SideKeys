// Harmonic analysis of a piece: key, roman numerals, functions, cadences, target notes. Pure.

export function analyzePiece(piece, { phraseLength = 4 } = {}) {
  throw new Error('not implemented');
}
