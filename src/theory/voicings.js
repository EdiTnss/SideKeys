// Voicing suggestions from templates, realized in a register. Pure, no DOM.

export const DEFAULT_REGISTER = [40, 64];   // E2–E4

export function suggestVoicings(chord, { register = DEFAULT_REGISTER, previous = null } = {}) {
  throw new Error('not implemented');
}
