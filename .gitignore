# Dependencies (only worker/ and mic-proto/ ever have any)
node_modules/

# Secrets and local state (Wrangler reads .dev.vars or .env, with per-environment variants)
worker/.dev.vars*
!worker/.dev.vars.example
worker/.env*
.wrangler/
*.local.json

# Product analysis (market, economics, legal) stays out of the repo; see docs/PRODUCT.md
/Voicing Lab ca produs.pdf

# OS files
Thumbs.db
.DS_Store
