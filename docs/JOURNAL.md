# SideKeys — JOURNAL.md

Jurnalul sesiunilor, mutat neschimbat din `CLAUDE.md` pe 2026-09-19. Intrările noi se adaugă la final; starea curentă și următorul pas stau în `CLAUDE.md`. Trimiterile la secțiuni din intrările vechi („vezi spec", „secțiunea de realizare") se referă la vechiul `CLAUDE.md`: specificațiile sunt acum în `docs/spec-*.md`.

**Proiectul s-a numit „Voicing Lab" până pe 2026-09-20** (vezi intrarea de la acea dată). Intrările de mai jos **nu** au fost rescrise: un jurnal e o consemnare, nu documentație, iar numele vechi din ele e ce s-a întâmplat atunci. La fel și sesiunile din `harness/sessions/`, care poartă `"app": "voicing-lab"` în antet, pentru că sub numele acela au fost înregistrate.

## Jurnal

- **2026-09-17** — Plan creat împreună cu Claude (chat). Decizii: repo în Claude Code, vanilla JS, fără dependențe, teste cu `node --test`, deploy GitHub Pages + Cloudflare Worker pentru AI. Următorul pas: Faza 0 — rulează `midi-test.html`, confirmă Genos în ambele direcții, inițializează repo-ul.
- **2026-09-17 (mai târziu)** — Adăugat „reharmonize piece": piesa intră ca grilă tastată + melodie cântată pe Genos și înregistrată cu poziția pe măsuri (Faza 2); Claude reharmonizează cu melodia fixă, `piece.js` validează determinist fiecare măsură (Faza 3). Import de fișiere MIDI și lead sheet din imagine — respinse deocamdată, posibil Faza 5.
- **2026-09-17 (seara)** — Reharmonizarea ridicată la arhitectură hibridă: codul face analiza armonică și generează candidații compatibili cu melodia, etichetați pe tehnică; Claude alege doar dintre candidați (`execute`), cu `plan` pe fraze și un `review` separat; scoruri deterministe; `realize.js` transformă grila în aranjament cântabil pe Genos. Faza 3 împărțită în 3a (hibrid minim) și 3b (pipeline complet + voicings). Faza 5 nouă: interactivitate, stiluri ca JSON, few-shot cu reharm-urile proprii, set de evaluare cu metrici și rating-uri. Următorul pas neschimbat: Faza 0.
- **2026-09-17 (noaptea)** — **Faza 0**: `midi-test.html` confirmat pe Genos în ambele direcții. Nume final Voicing Lab (`voicing-lab`), licență MIT, CLAUDE.md public. Repo local inițializat (README, `.gitignore`, `package.json` fără dependențe, LICENSE, primul commit); push după ce Edi creează repo-ul gol pe GitHub. **Decizii**: snapshot armat doar la note-on, nota de „next" interceptată; 60 = C4 peste tot (comentariul din `midi-test.html` corectat); m7 implicit funcție ii; caution ⊆ tensiuni disponibile; listele din tabel sunt complete (restul = wrong), regula generică doar pentru calități fără rând; sus = terța înlocuită cu 2/4, 11 = extensie cu terța prezentă; forma `C6/9`; `C13` = `C7` + 13; `b9` explicit = obligatoriu și 9 wrong; `alt` = 5/9/13 naturale wrong; câmp `required` per calitate; quartal cu 3M tolerată doar de la 4 note. Design aprobat pentru `notes.js` / `chords.js` (semnături + 10 teste); stub-uri și cele 10 teste scrise, roșii, necommise. **Deschise**: rândurile pentru `m6`, `6/9`, `sus4`; b5 obligatoriu pe m7b5 / dim7; ce e obligatoriu pe `alt`; regula b9 extinsă la #9 / b13 / 9 / 13; low interval limits (ce notă se compară, praguri vs tabel pe interval). **Următorul pas**: push, apoi implementarea `notes.js` și `chords.js` până trec cele 10 teste.
- **2026-09-17 (revizie înainte de push)** — Recitit tot planul. **Corectat**: regula de detecție drop 2/3/2&4 (se ridică nota cea mai de jos, nu „a doua de sus"); regula generică de avoid scoasă (tabelul e singura sursă; era moartă după decizia (a)). **Adăugat, aprobat de Edi**: snapshot pentru acorduri staccato (copia setului de dinaintea primului note-off); rootless A/B acceptă alterațiile pe dominante; „bas separat" (root/5 jos + tipul restului); 7 mare e tensiune disponibilă pe `6`; aliasuri Unicode și Real Book (`∆`, `º`, `Ø`, `MA7`, `MI7`), `Calt`, `b5`/`#5` pe dominante, `mMaj7`/triade/`+`/`7#5` în gramatică (rânduri încă deschise); `minorFunction` rămâne enum extensibil (iii, vi mai târziu); recorder-ul folosește `event.timeStamp` aliniat la `AudioContext`. Shell rămâne {3, 7} ± root deocamdată (Bud Powell R–7 / R–3 respins pentru moment). **Repo**: `.gitattributes` (LF), `.gitignore` extins (`.wrangler/`, fișiere de OS), Node ≥ 22, CI planificat în Faza 1. **Deschise, în plus**: rândurile pentru `mMaj7`, triade, `+`, `7#5`.
- **2026-09-17 (înainte de push)** — Edi a ridicat problema vizibilității („nu vreau să-mi fure cineva munca"). Decis: repo **privat până la Faza 4**, apoi public (Pages cere public pe cont gratuit); **fără licență** deocamdată (LICENSE șters, `"license": "UNLICENSED"`), decizia se ia la publicare. Regula „niciun secret în repo" e valabilă și cât e privat, pentru că istoricul devine public integral.
- **2026-09-17 (push)** — **Faza 0 bifată**: MIDI în ambele direcții, repo online (privat). GitHub raportează numele `Voicing-Lab` (cu majuscule); de decis dacă se redenumește în `voicing-lab` (recomandat, URL-ul Pages devine lowercase) sau se actualizează URL-urile din acest fișier. **Faza 1 începută**: commit `test:` cu cele 10 teste și stub-uri (roșu), apoi `feat:` cu `notes.js` (4/4 verzi). Urmează `chords.js` pe cele 6 teste rămase; rândurile deschise din tabel așteaptă răspunsurile lui Edi (întrebările a–j).
- **2026-09-17 (răspunsuri a–j)** — Nume păstrat `Voicing-Lab`; remote și URL-uri actualizate; corectată justificarea (Origin nu conține calea). **Decise**: `6/9` = rândul 6 cu 9 chord tone obligatoriu; `m6` cu tensiuni 9, 11, 7; `mMaj7` = 1 b3 5 7 (C Eb G B) cu tensiuni 9, 11, 13; b5 obligatoriu pe m7b5 și dim7; extensia scrisă explicit e obligatorie și face wrong celelalte forme ale treptei (b9/#9 coexistă); `7#5` = 7 + b13 obligatoriu, 5 și 13 wrong; low interval limits pe nota de jos a perechii, praguri ca tabel pe interval. **Deschise**: (1) convenția etichetelor — Edi citește `7` = septimă mică, `7+` = mare, `-7` = micșorată; tabelul și codul folosesc Berklee (`b7`, `7`, `bb7`); de decis dacă UI-ul afișează Berklee sau are opțiune de afișare europeană; (2) sensul lui `alt` — Edi: „alterat, adică mărit, 1 3 #5"; de clarificat dacă `7alt` rămâne dominanta alterată standard (b9 #9 #11 b13) cu 3, b7 și b13 obligatorii, sau înseamnă doar `7#5`; (3) aliasurile `7+` / `+7` (european = maj7, american = 7#5) și `-7` (american = m7) — de decis ce acceptă parserul; (4) tensiunile pe `sus4`, `maj`, `m`.
- **2026-09-17 (chords.js)** — **Decise**: etichete Berklee peste tot (opțiune de afișare europeană posibilă mai târziu); `7+` / `+7` respinse, `-7` = m7; tensiuni pe `sus4` (9, 13, b7; avoid 3), `maj` (7, 9, #11, 13; avoid 11), `m` (b7, 7, 9, 11, 13). **`chords.js` implementat, 10/10 teste verzi**: tabelul ca date, cea mai lungă potrivire pe aliasuri, prescurtări (`C9`, `Cm11`, `Cmaj13`, `C9sus4`, `Calt`), extensii explicite cu regula de excludere, `b5`/`#5`/`alt` doar pe dominantă, `classifyPc` ca unic loc de decizie. **Deschis**: `alt` — Edi a scris de două ori „C7alt = C E G# Bb"; de clarificat dacă b9/#9/#11 rămân disponibile (standard, implementat acum) sau devin wrong (alt = 7#5). Întrebare mică: `C°` / `Cdim` fără 7 ca alias pentru dim7? **Următorul pas**: testele parserului până la ≥ 20 de simboluri (prescurtări, extensii explicite, ii/i, `classifyPc`, erori), apoi `analyzer.js` test-first.
- **2026-09-17 (parser complet)** — **Decise**: `alt` = dominanta alterată standard cu 3, b7, b13 obligatorii (b9/#9/#11 rămân disponibile); `C°`/`Cdim` = dim7; repo confirmat Private. Parserul are **20/20 teste, 40+ simboluri** (DoD-ul Fazei 1 pentru parser e acoperit). Capcană JS găsită de un test: `Object.keys` pune cheile numerice (`'6'`, `'7'`) primele, deci ordinea tabelului e ținută explicit în `QUALITY_IDS`, verificată la încărcare față de `QUALITIES`. **Următorul pas**: `analyzer.js` — propunere de semnătură, formă a ieșirii și primele teste, apoi implementare după confirmarea lui Edi.
- **2026-09-17 (analyzer)** — Design aprobat de Edi; **`analyzer.js` implementat, 34/34 teste** (14 noi: fiecare tip de voicing cu caz pozitiv și negativ, cum cere DoD-ul). Schimbare de spec descoperită la scrierea testelor: quartal se verifică înaintea drop-urilor (4 cvarte suprapuse sunt și un drop 2). Poziție strânsă = întindere ≤ 12 semitonuri. Din DoD-ul Fazei 1 rămân: `midi/input.js` (snapshot), UI-ul de drill cu claviatură SVG, `index.html`, tasta „next", sesiunea de 20 de minute. **Următorul pas**: propunere pentru `input.js` + UI minimal, apoi implementare.
- **2026-09-17 (drill-ul rulează)** — Aprobat de Edi: captura ca `midi/capture.js` (logică pură, 9 teste pe ceas fals) + `midi/input.js` (cablaj Web MIDI, `parseMidiMessage` testat), „next" = Space sau E1 (MIDI 28, configurabil). Implementat: `ui/drill.js` (setări persistate, teste), `ui/keyboard.js` (SVG E1–G7, 76 de clape ca Genos, colorate pe rol), `ui/render.js`, `app.js`, `index.html`, `styles.css`; CI pe GitHub Actions (Node 22/24) cu badge; hook de debug `window.voicingLab.play(...)` pentru consolă fără clapă. Verificat în browser: rootless A recunoscut, avertismentele în ordine, Space și E1 avansează. **48/48 teste.** Structura din plan s-a schimbat: `midi/capture.js` nou, `src/app.js` e cablajul, `styles.css` la rădăcină (folderul `styles/` rămâne pentru profilurile JSON din Faza 5). **Următorul pas**: sesiunea de 20 de minute a lui Edi pe Genos (DoD Faza 1); bug-urile și observațiile lui intră ca teste.
- **2026-09-17 (Faza 1 bifată)** — Edi a făcut sesiunea pe Genos: fără bug-uri, fără consolă. **DoD Faza 1 îndeplinit** (parser 40+ simboluri, fiecare tip de voicing cu caz pozitiv și negativ, 49 de teste). Singura cerere: în UI, half-diminished și diminished se afișează cu simbolurile lor, `ø7` și `°` fără 7, pentru că simbolul de diminished implică deja septima micșorată (ids-urile interne rămân `m7b5` / `dim7`; setările salvate cu numele vechi migrează). **Următorul pas**: Faza 2, în ordinea: `progressions.js` + parser de grilă text → voice leading (`compareVoicings`) → metronom Web Audio + modul progresie în UI → `output.js` (MIDI out, canalul 1, confirmat în Faza 0) → `recorder.js` + `piece.js` → statistici de sesiune.
- **2026-09-17 (Faza 2, pasul 1)** — Notație finală în drill: `ø7` și `°` (Edi: doar diminished pierde 7-ul). Ordinea Fazei 2 și biblioteca de progresii aprobate. **`progressions.js` implementat, 57/57 teste**: `parseGrid` / `formatGrid` (împărțire egală sau eroare cu numărul măsurii, `%`), `transposeSymbol` / `transposeGrid` (ortografie după tonalitate), `PROGRESSIONS` (ii-V-I, ii-V-i cu Cm6, blues, jazz blues, rhythm changes A, turnaround, Coltrane) și `getProgression(id, key)`. **Următorul pas**: pasul 2, `theory/voiceLeading.js` cu `compareVoicings`, după confirmarea designului.
- **2026-09-17 (Faza 2, pasul 2)** — Designul pașilor 2 și 3 aprobat (pasul 3 cu ambele moduri, free și timed). **`voiceLeading.js` implementat, 64/64 teste**: potrivire cu deplasare minimă (sortată, nu greedy — vezi spec), rating smooth/ok/jumpy, `scoreProgression` cu toleranță pentru schimbările de registru între voicings spread. **Următorul pas**: pasul 3 — `theory/timing.js` (timp ↔ bar/beat, testat), `audio/metronome.js`, modul progresie în UI.
- **2026-09-17 (Faza 2, pasul 3)** — Designul pasului 4 aprobat. **Pasul 3 implementat, 75/75 teste**: `timing.js` (positionAt / timeOf / quantizePosition, cu count-in), captura raportează `startedAt` (primul note-on), `ui/session.js` (sloturi, `locate` cu chorus-uri, cursor free, istoric, `summary`), `audio/metronome.js` (scheduler cu lookahead, click accentuat, count-in, `positionOf` prin `getOutputTimestamp`), UI cu tab-uri Drill / Progression: bibliotecă + grilă editabilă + tonalitate + măsură + tempo + loop, moduri free și timed, grila cu slotul curent și ✓/! pe cele cântate, raport de voice leading la fiecare chorus. Verificat în browser în ambele moduri. **Următorul pas**: pasul 4 (`theory/voicings.js`, `midi/output.js`, buton Play suggestion) și testul lui Edi pe Genos în modul timed.
- **2026-09-17 (Faza 2, pasul 4)** — Designul pașilor 5 și 6 aprobat. **Pasul 4 implementat, 83/83 teste**: `theory/voicings.js` (șabloane, registru E2–A4 în loc de E2–C4 — vezi secțiunea de realizare), `midi/output.js` (mesaje pure testate, `createOutput` cu port și canal), Settings → MIDI out (port + canal, persistate), buton „Play suggestion" / tasta P (apăsată din nou trece la următorul candidat), sugestia conturată punctat pe claviatură peste ce a cântat Edi. Bug-uri găsite la verificarea în browser și rezolvate: panoul de progresie rămânea vizibil în Drill (`display: grid` bătea `hidden`); ordinea sugestiilor favoriza shell-urile de 2 note (acum: întâi aceeași textură). **Următorul pas**: pasul 5 (`theory/piece.js`, `midi/recorder.js`, Record melody / Save / Load / Export), apoi pasul 6 (statistici); de la Edi: testele pe Genos ale modului progresie și ale sugestiilor (sunet pe canalul 1, eventual ecou MIDI înapoi în aplicație).
- **2026-09-17 (Faza 2, pasul 5)** — **Pasul 5 implementat, 94/94 teste**: `theory/piece.js` (model, `addMelody` cu cuantizare la optimi, nota se termină unde începe următoarea, < 60 ms doar în `raw`, note-țintă = timp tare sau ținută ≥ 1 timp și cel puțin o optime; JSON validat; `savePiece` / `loadPiece` / `listPieces` / `deletePiece` în `localStorage`), `midi/recorder.js` (timp muzical, poziții injectate, testat cu ceas fals), UI în panoul de progresie: Record melody (întotdeauna cu metronom, un chorus, oprire automată; captura e pauzată cât se înregistrează), melodia sub grilă (țintă îngroșat, passing estompat), Save (titlu), Load, Delete, Export JSON, Import JSON. Verificat în browser cu note simulate. **Următorul pas**: pasul 6 (`ui/stats.js`), apoi sesiunea DoD a Fazei 2 a lui Edi.
- **2026-09-17 (Faza 2, pasul 6)** — **Pasul 6 implementat, 98/98 teste**: `ui/stats.js` (obiecte imutabile; pe calitate: încercări, curate, trepte lipsă, note greșite, avoid; `weakSpots`, `summaryLine`, `loadStats`/`saveStats` cu try/catch), linia „Session" în antet, cumulativul cu Reset în Settings; fiecare analiză din drill sau progresie intră în ambele. **Toată Faza 2 e implementată**; rămâne DoD-ul, care e al lui Edi: sesiune cu metronom pe jazz blues cu raport de voice leading, o sugestie auzită pe Genos, o piesă înregistrată, salvată și reîncărcată. **Următorul pas**: sesiunea lui Edi; observațiile intră ca teste; apoi Faza 3a (Worker + `ai/client.js` + `analysis.js` / `candidates.js` / `scoring.js`).
- **2026-09-17 (Faza 2 bifată)** — Bug de UI găsit de Edi și rezolvat: dropdown-urile erau ilizibile în tema întunecată pe Windows (fundal alb, text alb moștenit) → culori de sistem `Canvas`/`CanvasText`. Sesiunea DoD pe Genos: jazz blues cu metronom și raport, sugestie auzită pe canalul 1, piesă înregistrată/salvată/reîncărcată — „totul merge bine". Genos **nu** trimite notele primite înapoi (fără ecou), deci nu e nevoie de filtru la MIDI out. **DoD Faza 2 îndeplinit**, 98 de teste. **Următorul pas**: Faza 3a, în ordinea: `analysis.js` → `candidates.js` → `scoring.js` (pure, testabile, fără cont) → `worker/` + `ai/client.js` + `ai/prompts.js` + explain & suggest → pipeline `execute` + UI de reharm. Pregătire pe partea lui Edi: cont Cloudflare și cheie Anthropic (doar în `worker/.dev.vars` și în secretele Worker-ului).
- **2026-09-17 (Faza 3a, pasul 1)** — Ordinea Fazei 3a și designul aprobate (blues: I7 = T, IV7 = S, cadență doar pe V7). **`analysis.js` implementat, 106/106 teste**, pe 8 piese construite manual: ii–V–I cu dominantă secundară, jazz blues în F, ii–V–i minor, tritonal, dim de trecere / iv / backdoor / împrumuturi, pasaj modal fără tonalitate (ghicită), relațiile melodiei, frazele. Spec precizat: backdoor = cadență; I7 din blues nu devine V7/IV. **Următorul pas**: pasul 2, `candidates.js`, după confirmarea designului.
- **2026-09-17 (Faza 3a, pasul 2)** — Design aprobat; avoid pe notă-țintă = păstrat cu warning. **`candidates.js` implementat, 117/117 teste** (un caz pozitiv și unul negativ pe fiecare tehnică, filtre de intensitate, ordonare pe stil, limită). Descoperit la teste: limita de 12 era umplută de schimbările de calitate → selecție round-robin pe tehnici; bug găsit de test: fundamentala lui ii se calcula cu o cvartă în loc de o cvintă. **Următorul pas**: pasul 3, `scoring.js`, după confirmarea designului; apoi pasul 4 (Worker + client + prompts), care are nevoie de contul Cloudflare și de cheia API ale lui Edi.
- **2026-09-17 (Faza 3a, pasul 3)** — Design aprobat (tabelul de costuri al basului inclus). **`scoring.js` implementat, 123/123 teste**, pe grile artificiale: totul original, o substituție tritonală, tabelul de costuri al basului (inclusiv bas de după `/`), clash-uri recalculate din melodie (un acord construit manual ca să se bată cu melodia dă `clashes = 1`; jumătățile unui `related-ii` verificate separat), țintele de densitate pe toate intensitățile, `maxRun` 5 respins sub `heavy`, Coltrane numărat ca două sloturi cu alegerea de pe slotul acoperit ignorată. Pe partea de mediu: plugin-ul Cloudflare pentru Claude Code instalat la nivel de utilizator (skill-urile `wrangler` și `workers-best-practices`, server MCP cu login la prima folosire); CLI-ul aplicației desktop e în `AppData\Roaming\Claude\claude-code\<versiune>`, nu pe PATH. **Următorul pas**: pasul 4 — `worker/` (proxy cu verificare `Origin` și limită pe IP, `wrangler` ca dev dependency izolată, `.dev.vars.example`), `ai/client.js`, `ai/prompts.js` cu `PROMPT_VERSION`, explain & suggest din drill; are nevoie de contul Cloudflare și de cheia Anthropic (doar în `worker/.dev.vars` și în secretele Worker-ului). Apoi pasul 5 (pipeline `execute` + UI de reharm + testul DoD cu `candidateId` inexistent).
- **2026-09-18 (Faza 3a, pasul 4)** — Decizii Edi: Worker-ul apelează Anthropic prin `fetch` direct (fără SDK), modelul e `claude-opus-5`. **Pasul 4 implementat, 140/140 teste** (laptopul lui Edi a dat blue screen la mijloc; repo-ul a rămas intact, verificat cu `git fsck` și cu testele, apoi împins): `worker/` (proxy testat sub `node --test` cu `env` fals; `wrangler` 4.134 ca dev dependency izolată, `wrangler.jsonc` cu binding-ul `ratelimits`, `wrangler deploy --dry-run` și `wrangler dev` verificate local, `.dev.vars.example`; `.gitignore` acoperă `worker/.dev.vars*` și `worker/.env*`), `ai/client.js` (erori pe tipuri), `ai/prompts.js` (`explain` și `execute` versionate, scheme în subsetul structured outputs), `ai/explain.js` (validare prin analizor = DoD 1 ca test), UI: setarea `proxyUrl`, butonul „Ask Claude" / A, tastele 1 / 2. Verificat în browser: fără URL → avertisment; cu `wrangler dev` fără cheie → „Claude: proxy not configured" afișat curat; calea de succes simulată cu un `fetch` fals → lista randată, duplicatul respins cu motiv, tasta 1 conturează clapele, voicing-ul cântat e recunoscut ca tipul promis, blocul rămâne. Capcană de mediu: npm 11 blochează scriptul de post-instalare al `workerd` („allow-scripts"), dar binarul vine din pachetul de platformă și `wrangler dev` merge. **Rămâne pe partea lui Edi**: contul Cloudflare, cheia Anthropic (în `worker/.dev.vars` local și `npx wrangler secret put ANTHROPIC_API_KEY` în cloud), `npm run deploy` din `worker/`, URL-ul Worker-ului în Settings, apoi DoD (1) pe Genos. **Următorul pas**: pasul 5 — `ai/pipeline.js` (`execute`: candidați → apel → validare id-uri → `resolveChoices` → scoruri), validarea din `piece.js`, UI de reharm cu A/B și scoruri, testul DoD (3) cu `candidateId` inexistent.
- **2026-09-18 (Faza 3a, pasul 5)** — Decizii Edi: UI-ul de reharm într-un **tab nou**, iar A/B **trimite grila aleasă în Progression**. **Pasul 5 implementat, 149/149 teste**: `ai/pipeline.js` (`reharmonize`, cu `candidates` ca opțiune pentru lock & regenerate), `checkMelody` + `validateReharm` în `piece.js` (`scoring.js` nu-și mai ține propria copie a verificării melodiei), desfășurarea grilei înapoi în text cu `gridParses`, tab-ul Reharm complet. **DoD (3) bifat**: un `candidateId` inexistent lasă slotul pe original și intră în `problems`, la fel un răspuns fără `bars`, o intrare fără coordonate, un slot inexistent, un slot răspuns de două ori și o alegere pe un slot acoperit. Bug găsit la verificarea în browser: helper-ul `h()` din `render.js` scrie și atributele cu valoare `undefined`, deci `disabled: undefined` dezactiva butonul; acum se pune pe proprietate, nu pe atribut. **Toată Faza 3a e implementată**; rămân DoD-urile (1) și (2), care sunt ale lui Edi, după ce publică Worker-ul. **Următorul pas**: Edi pune cheia și face deploy, apoi sesiunea DoD pe Genos (alternativă cerută din drill, recunoscută de analizor; piesă de 16 măsuri înregistrată, reharmonizată „medium / tritone", exersată în Progression). Observațiile lui intră ca teste. Apoi Faza 3b: `plan` și `review` în pipeline, execuție pe fraze peste 32 de măsuri, `realize.js`.
- **2026-09-18 (Faza 3a, probe pe cheia reală)** — Cheia lui Edi merge (`npm --prefix worker run check-key`, 11 modele, `claude-opus-5` disponibil). Probat cu Worker-ul local: un apel direct prin proxy, „Ask Claude" din drill (două voicings, ambele acceptate de analizor, tipul detectat = cel promis) și o reharmonizare de 16 măsuri în F, stil tritonal, intensitate medie. **Cifre de referință pentru o piesă de 16 măsuri**: 17 sloturi, 204 candidați, prompt ~19 000 de caractere, ~10 900 token-uri de intrare și ~1 400 de ieșire, 16 secunde, în jur de 9 cenți; `clashes` 0, densitate 41% în țintă, `maxRun` 2, bas 0,78, trei tehnici. Găsit la rulare: modelul scurtează uneori id-ul candidatului, iar slotul se pierdea degeaba → recuperare după simboluri (`repairs`) și regulă explicită în prompt, `execute` la `PROMPT_VERSION` 2. **Ergonomie**: `start.cmd` plus o scurtătură pe desktop pornesc pagina, proxy-ul și Chrome dintr-un dublu-clic; pe localhost adresa proxy-ului se completează singură în Settings. Terminalul lui Edi e PowerShell, deci comenzile pentru el nu folosesc `&&`.
- **2026-09-18 (Faza 3a bifată)** — Edi a făcut sesiunile pe Genos: „e ok totul". Jurnalul Worker-ului local (baza de observabilitate a lui `wrangler dev`, interogabilă din `/cdn-cgi/local/explorer/api/local/observability/query`) arată în dimineața de 18 septembrie două `explain` și două `execute`, toate cu status 200. Mărimea de 16 măsuri e acoperită și de proba mea din seara precedentă (`clashes` 0, densitate în țintă). **DoD Faza 3a îndeplinit**: (1) și (2) pe Genos de Edi, (3) prin test automat. 150 de teste. De notat pentru 3b: filtrarea candidaților după intensitate (la `light` doar tehnicile permise) e deja în `candidates.js`, deci punctul acela din Faza 3b e făcut. **Următorul pas**: Faza 3b, după confirmarea ordinii și a designului.
- **2026-09-18 (Faza 3b, pasul 1)** — Decizii Edi: întâi aranjamentul, basul ca fundamentală ținută, trei canale configurabile, mâna stângă fără dublarea melodiei. **Implementat, 163/163 teste**: `theory/realize.js`, `midi/player.js`, `output.sendScheduled` / `silence`, `metronome.performanceTimeOf`, canalele pe voce în Settings, „Play original" / „Play reharm" / „Stop" și bifele pentru voci în tab-ul Reharm, `reharmonized` în rezultatul pipeline-ului. Verificat în browser cu o pagină temporară care dădea aplicației un port MIDI fals, ca să nu sune nimic pe Genos (panoul de browser al lui Claude nu are permisiune Web MIDI; pagina a fost ștearsă, nu e în repo): 50 de mesaje pentru 4 măsuri, primul la ~2,1 s (numărătoarea), ultimul la ~10,1 s, fiecare voce pe canalul ei, mâna stângă oprită dispare de pe canalul 2 și alegerea se păstrează, Stop trimite `clear()` și All Notes Off pe canalele 1–3, claviatura aprinde voicing-ul care sună. Găsit la verificare și reparat test-first: când o notă a melodiei era inevitabilă (F-ul din G7), regula „evită dublarea" renunța cu totul și G7 lua G2 D3 F3 B3, dublând și G-ul; acum alege varianta cu cele mai puține dublări (B2 E3 F3 A3). **De la Edi**: pe Genos, canalele 2 și 3 primesc o voce (pian și bas), apoi „Play original" și „Play reharm" pe o piesă înregistrată — asta e jumătatea „îl auzi pe Genos" din DoD-ul Fazei 3b. **Următorul pas**: `plan` și `review` în pipeline, cu comparația măsurată față de 3a (`bassSmoothness`, `techniqueMix`), apoi execuția pe fraze peste 32 de măsuri.
- **2026-09-18 (Faza 3b, redarea pe Genos)** — Edi a ascultat aranjamentul pe Genos: „merge bine", dar basul suna prea jos și „muddy". Cauza, găsită de Edi: **vocea de bas de pe Genos era setată cu o octavă mai jos**; registrul din cod (E1–D#2) era corect. Între timp am încercat basul o octavă mai sus, iar încercarea a scos la iveală două probleme reale, valabile în orice registru, reparate test-first: mâna stângă putea coborî sub bas (acum stă mereu deasupra lui, la un interval curat), iar un shell forțat de un loc strâmt se lipea de toate acordurile următoare (acum textura plină are prioritate; pe progresia de probă, cu basul sus, 5 din 8 acorduri au 4 note, față de unul singur înainte). Registrul implicit a revenit la E1–D#2, cu A1–G#2 și E2–D#3 ca opțiuni în Settings. **166 de teste.** Edi a adăugat în repo un folder `assets/` (logo, favicon, imagine OG, `BRAND.md`), necommis, pentru Faza 4; neatins. **Următorul pas**: `plan` și `review` în pipeline, cu comparația măsurată față de 3a, apoi execuția pe fraze peste 32 de măsuri.
- **2026-09-19 (Faza 3b, pasul 2: plan și review)** — Decizii Edi: un review care strică o țintă atinsă de ciornă se anulează întreg; bifa „plan & review" e activă implicit; măsurarea cu 3 rulări pe mod. **Implementat, 179 de teste**: `plan` (fraze verificate contra piesei și a meniului), `execute` v3 cu planul, `review` (cel mult 4 schimbări, gardă, ciorna păstrată la eșec), UI-ul din tab-ul Reharm, `eval/compare.js`. Verificat în browser cu un proxy simulat: stadiile cu secundele, planul, cele două rânduri de scoruri, verdictul, măsurile revizuite și motivul lor, anularea afișată, bifa ținută minte. **Măsurat pe piesa de 16 măsuri, tritone / medium, 3 rulări pe mod** (`eval/reports/compare-2026-09-19T13-59-52-398Z.json`): bas **0,85** (0,83–0,90) cu plan + execute + review față de **0,80** (0,78–0,83) cu execute singur; mix de tehnici **4,67** (4–5) față de **3,67** (3–4); `clashes` 0 și densitatea în țintă în toate cele 6 rulări, nicio problemă, nicio recuperare de id. **DoD-ul măsurat al Fazei 3b e îndeplinit.** Cea mai mare parte a câștigului vine din plan (ciorna are deja bas 0,85 și mix 4,0); review-ul a adăugat varietate (a schimbat 0, 2 și 1 măsuri), nu bas. Preț: pipeline-ul complet ia ≈ 59 s și ≈ 25 de cenți pe o piesă de 16 măsuri, față de ≈ 17 s și ≈ 9 cenți pentru execute singur (estimasem 35–40 s și ≈ 20 de cenți; review-ul singur ia ≈ 29 s); măsurarea a costat ≈ 1,02 $, peste estimarea de ≈ 90 de cenți. Trei rulări pe o singură piesă și un singur stil sunt un semnal, nu o statistică; setul din Faza 5 o va confirma sau nu. **De la Edi**: o piesă proprie cu „plan & review" în tab-ul Reharm, ascultată pe Genos (jumătatea „îl auzi" a DoD-ului, cu pipeline-ul complet). **Următorul pas**: execuția pe fraze pentru piese peste 32 de măsuri, cu contextul frazei precedente, apoi Faza 3b e completă.
  - **Stare la închiderea sesiunii**: `main` = `origin/main`, 179 de teste verzi, niciun proces pornit (Worker-ul și serverul de previzualizare oprite, porturile 8787 și 3000 libere pentru `start.cmd`). Necommis doar `assets/` (al lui Edi, pentru Faza 4). Worker-ul încă nu e publicat în cloud (`npx wrangler login`, `npm --prefix worker run deploy`, `npm --prefix worker run secret`), decizia lui Edi, cel târziu în Faza 4.
  - **De decis cu Edi înainte de execuția pe fraze** (designul nu e aprobat încă): cât cuprinde un apel `execute` (o frază de 4 măsuri sau bucăți de până la 32 de măsuri, tăiate la granițe de frază); contextul trimis bucății următoare (spec-ul zice ultimele 2 acorduri alese; poate și scorurile parțiale, ca densitatea să rămână în țintă pe toată piesa); `plan` rămâne un singur apel pe toată piesa; `review` o singură rundă pe toată piesa (spec: fără bucle) sau câte una pe bucată; cum se testează fără cheie (clientul fals din `test/pipeline.test.js` răspunde deja pe acțiune) și pe ce piesă lungă se măsoară.
- **2026-09-19 (documentația spartă)** — Planul de produs s-a schimbat: `docs/PRODUCT.md`, scris de Edi, e sursa de adevăr pentru scop; reharm-ul iese din produs și rămâne în repo și în demo. Făcut un punct din P0, fără cod: `CLAUDE.md` spart în reguli, jurnal și specificații. Mutate neschimbat (verificat cu `diff` pe intervalele de rânduri): jurnalul în `docs/JOURNAL.md`, specificația analizorului în `docs/spec-analyzer.md`, a reharmonizării în `docs/spec-reharm.md`, captura în `docs/spec-capture.md`, fazele 0–5 și vechiul scope guard în `docs/PHASES.md`. În `CLAUDE.md` au rămas scopul (cu prioritate pentru `PRODUCT.md`), regulile de colaborare, convențiile, stack-ul, structura, starea curentă, următorul pas și lista documentelor. **Regulă nouă**: `PRODUCT.md` se citește la începutul fiecărei sesiuni, specificațiile doar când se lucrează la modulul lor; jurnalul se scrie la final și nu se mai citește la început. Cu acordul lui Edi, în același commit: comentariile din `src/midi/capture.js` și `src/theory/chords.js` trimit acum la `docs/spec-capture.md` și `docs/spec-analyzer.md`, iar `Voicing Lab ca produs.pdf` e în `.gitignore` (analiza de piață nu intră în repo). **Rămas deschis**: „Scop dublu" și stack-ul din `CLAUDE.md` (fără framework, fără bundler) contrazic P1 din `PRODUCT.md`, iar vechiul scope guard contrazice și P0 (intrarea pe microfon). **Următorul pas**: execuția pe fraze, după aprobarea designului.
- **2026-09-19 (P0 începe: execuția pe bucăți)** — **Ordinea P0 aprobată de Edi**: execuția pe fraze (închide 3b, apoi `pipeline.js` îngheață) → harness-ul de verificare (port MIDI fals + sesiuni reluate; e și intrarea injectată pe care o va folosi claviatura de pe ecran) → demo mode (claviatură, `audio/synth.js`, piesa demo; de decis atunci: reharm-ul din demo ca rezultat pre-calculat, ca un vizitator să nu coste 9–25 de cenți) → publicarea (istoric fără secrete, licență, repo public, Pages, Worker, README, GIF, video) → prototipul de microfon (în afara lui `src/`, pentru că `basic-pitch-ts` e o dependență npm; adevărul de referință vine din MIDI-ul înregistrat în același timp cu sunetul). **Măsurat înainte de design**: corpul cererii `execute` are 24 KB la 17 sloturi, 46 KB la 34 și 89 KB la 68, iar Worker-ul refuză peste 64 KB; `review` e cu ~13% mai mare. Deci limita ține de sloturi, nu de măsuri: o piesă de 32 de măsuri cu două acorduri pe măsură pica deja. **Decizii Edi** (toate recomandările mele): tăiere la 32 de sloturi, la granițe de frază; aceeași țintă de densitate pe fiecare bucată; un review pe bucată, în paralel, cu garda pe piesa întreagă; o bucată eșuată rămâne pe original și rularea continuă; piesa lungă o scriu eu și o verifică el. **Implementat, 186 de teste** (7 noi, scrise întâi și commise roșii): `splitIntoParts`, `execute` v4 cu `part` (`previousChords`, `changedInARowBefore`), `review` v2 pe bucată, planul pe toată piesa cu plafonul întreg (8192), stadiul și verdictele pe bucăți în tab-ul Reharm, `parts` în raportul lui `compare.js`, `MAX_BODY_BYTES` exportat din Worker și verificat de un test pe cel mai rău caz (64 de măsuri, două acorduri pe măsură, meniuri pline: 22 KB `plan`, 43 KB `execute`, 47 KB `review`). O piesă de până la 32 de sloturi face exact apelurile de dinainte; cele 17 teste vechi ale pipeline-ului au rămas neschimbate. Verificat în browser cu proxy simulat: stadiile numesc bucata, review-urile pleacă deodată, al doilea e anulat singur când strică densitatea, o bucată trunchiată rămâne pe original cu problema afișată, o piesă scurtă arată ca înainte. Găsit la teste: o greșeală de calcul în testul meu (run-ul peste graniță avea 10 sloturi, nu 6), codul era corect. **Piesa de măsurare**: `eval/pieces/study-in-bb-aaba.json`, studiu original, 64 de măsuri AABA în Bb, 73 de sloturi, 3 bucăți (1–24, 25–44, 45–64), cereri de 18–39 KB; construită cu `createPiece` + `addMelody`, toate notele-țintă sunt note ale acordului sau tensiuni pe grila originală. Necommisă până o verifică Edi. Corectat în spec: plafonul de tokeni al Worker-ului e 8192, nu 4096. În timpul sesiunii a apărut `docs/claude-setup/` (gardă de sesiune, `/review`, `/handoff`), neatins, neactivat. **Următorul pas**: Edi verifică piesa (grila, melodia, eventual ascultată cu Import JSON) și confirmă bugetul (~5 $ pentru 3 rulări pe mod, sau ~3,6 $ doar modul complet, plus ~0,35 $ o rulare de control pe `study-in-f`), apoi măsurarea; cu ea, Faza 3b e completă.
- **2026-09-19 (măsurarea pe piesa lungă)** — Edi a aprobat piesa și bugetul de ~4,5 $ (3 rulări pe mod; rularea de control pe `study-in-f` nu intra în sumă, deci n-am făcut-o). **Bug găsit la pornirea Worker-ului și reparat înainte de push**: exportul lui `MAX_BODY_BYTES` din `worker/src/index.js` oprea workerd la pornire („Incorrect type for map entry"), pentru că runtime-ul citește fiecare export numit al modulului principal ca punct de intrare; testele nu-l prindeau, fiindcă rulează handler-ul în Node, nu în workerd. Constanta stă acum în `worker/src/limits.js`, iar un test nou verifică că modulul principal exportă doar handler-ul și funcții. **187 de teste.** **Măsurat** (`eval/reports/compare-2026-09-19T16-37-52-096Z.json`, 64 de măsuri, 73 de sloturi, 3 bucăți, tritone / medium): toate 6 rulările au 0 clash-uri, densitatea în țintă (41–47%), `maxRun` 1–3, nicio cerere refuzată sau trunchiată, nicio problemă. Bas **0,90** cu plan + execute + review față de **0,84** cu execute singur, mix de tehnici **4,67** față de **3,67**; ca la 16 măsuri, câștigul vine din plan. Timp ≈ 82 s față de ≈ 156 s (review-urile în paralel au scurtat rularea completă cu ≈ 67 s); cost ≈ 0,41 $ față de ≈ 1,00 $; toată măsurarea 4,23 $. **Granițele**: 24 → 25 curată în toate rulările; 44 → 45 e Gb7 → Cm7 (triton în bas) în toate 6, dar planul, care vede toată piesa, l-a cerut explicit în toate 3 rulările cu justificări greșite („coboară un semiton în Cm7-ul următor"), iar review-ul l-a lăudat: e o greșeală a modelului pe II7 → ii7, nu un efect al tăierii. Codul o vede (costul de bas), modelul nu primește `bassStepToNext`; notat în spec pentru Faza 5, fără lucru acum (reharm-ul e înghețat). **Toate punctele Fazei 3b sunt implementate și măsurate**; din DoD mai rămâne partea lui Edi: o piesă proprie cu „plan & review", ascultată pe Genos. `pipeline.js` e înghețat de acum. Worker-ul local oprit cu tot arborele de procese; porturile 8787 și 3000 libere. **Următorul pas**: harness-ul de verificare (port MIDI fals + sesiuni înregistrate care se reiau), după aprobarea designului.
- **2026-09-19 (seara: harness-ul de verificare)** — Împinse pe GitHub cele 6 commit-uri ale execuției pe bucăți; rularea de control pe `study-in-f` sărită (decizie Edi). **Design aprobat de Edi** (toate recomandările): portul MIDI virtual, înregistrarea mereu pornită în memorie cu „Save session", notele și pedalele, reluarea în Node și CI cu verdictele ca referință, sesiunile lui Edi în repo, „raportează" = raport de regresie. **Implementat, 206 teste** (19 noi, commise întâi roșii): `midi/virtual.js` și `connectMidi({ access, onMessage })`; `?midi=virtual` în aplicație; `ui/tape.js` (banda, `verdictOf`, `replayTape`, `updateTape`, `formatReport`, `formatSession`), în `ui/` și nu în `midi/` cum spusesem, pentru regula de dependențe; `snapshotTarget` scos din `app.js` în `ui/session.js`, folosit de aplicație și de reluare; butonul „Save session"; `harness/replay.js` (`--update`) și `test/replay.test.js`. **Proba de cap la cap**: în panoul de browser, pe portul virtual, am cântat 6 acorduri în drill, o trecere free și una timed; sesiunea înregistrată de aplicație (175 de evenimente) a ajuns în `harness/sessions/` printr-un receptor local de unică folosință și se reia cu toate cele 13 snapshot-uri la fel. O limită de interval inventată, adăugată doar în memorie, iese în raport ca exact 4 verdicte schimbate. **Găsit pe drum**: (1) fereastra pentru timer-ul întârziat live era 100 ms și ascundea chiar schimbările pe care trebuie să le prindă (un debounce de 30 ms trecea drept „13 same"); măsurat pe sesiune, timer-ul live întârzie 1,6–15,3 ms, deci fereastra e acum 25 ms, cu un test. (2) **Bug în modul timed, confirmat live**: un acord anticipat (apăsat înainte de bara de măsură și ținut peste ea) nu primește niciun verdict, pentru că schimbarea de slot anulează captura; iar dacă n-ar anula, ar fi judecat pe acordul dinainte (poziția vine din prima notă). De decis cu Edi, apoi reparat și verificat pe sesiunile înregistrate. (3) `tools/quiet-report.mjs` (al lui Edi, necommis) nu merge pe Node 24: caută sumarul TAP, dar Node scrie implicit `spec`; se repară cu `--test-reporter=tap` în argumentele lui `spawnSync`. Nu l-am modificat. **De la Edi**: 2–3 sesiuni reale pe Genos (drill ~5 min, jazz blues timed 2 chorusuri, o progresie free), salvate cu „Save session"; cu ele, DoD-ul harness-ului din P0 e îndeplinit. **Următorul pas**: sesiunile lui Edi și decizia despre anticipare, apoi demo mode.
- **2026-09-19 (noaptea: prima sesiune reală)** — Împinse cele 5 commit-uri ale harness-ului. Edi a înregistrat pe Genos o sesiune de 11 minute (drill, jazz blues timed la 120, rhythm changes free) și a salvat-o cu „Save session": 1160 de evenimente, 93 de snapshot-uri, doar note pe canalul 1. **Prima reluare a găsit o problemă în reluare, nu în aplicație**: 92 din 93 la fel, iar un Ebm7 arpegiat lent (321 ms între primele două note) ieșea „missing + extra" cu același verdict. Live, timer-ul primei note rulase cu o singură notă apăsată, ceea ce nu lasă nimic pe bandă, dar mută începutul snapshot-ului pe nota a doua; reluarea luase tăcerea drept timer întârziat. Reparat cu dovada de pe bandă (un snapshot înregistrat care începe exact pe nota care vine), cu un test din cazul real; acum 93 din 93, iar codul de dinainte dă 1 + 1 pe aceeași sesiune. Sesiunea e în `harness/sessions/genos-2026-09-19-drill-blues-rhythm.json` (copiată, originalul a rămas în Downloads). **DoD-ul harness-ului din P0 e îndeplinit**: reia o sesiune reală și raportează. **208 teste.** În partea timed (29 s) nu apare nicio anticipare: toate cele 24 de snapshot-uri încep pe bătaie sau după, deci bug-ul de anticipare n-a lovit sesiunea; decizia despre prag rămâne deschisă. **`tools/quiet-report.mjs` reparat** la cererea lui Edi: cere explicit `--test-reporter=tap` (din Node 23 reporter-ul implicit e `spec`); verificat pe suită (o linie) și pe un test care pică intenționat (numele și diff-ul, cod de ieșire 1). Scriptul, `test:quiet` din `package.json` și rândurile lui din `CLAUDE.md` sunt ale lui Edi, încă necommise; comentariile și mesajele scriptului sunt în română, iar regula repo-ului cere engleză în cod. **Următorul pas**: pragul pentru acordurile anticipate în timed, apoi demo mode.
- **2026-09-19 (noaptea, târziu: anticiparea)** — La cererea lui Edi: `tools/quiet-report.mjs` tradus în engleză și commis cu `test:quiet` și rândurile lui din `CLAUDE.md`; împinse toate commit-urile. **Decizie Edi**: anticiparea începe de la „și"-ul lui 4. Implementată test-first ca regulă generală: `ANTICIPATION_BEATS` = 0,5 în `ui/session.js`, `snapshotTarget` se uită cu jumătate de timp înainte de unde a început voicing-ul, pe orice schimbare (și la mijlocul măsurii, și în 3/4), cu primul acord anticipabil din numărătoare și ultimul păstrându-și voicing-ul fără loop; schimbarea de slot făcută de metronom nu mai anulează captura, iar banda o marchează `cancels: false`, ca sesiunile vechi să se reia cum au fost înregistrate. Verificat în aplicație pe portul virtual (serverul de pe 3000 era al lui Edi, din `start.cmd`, lăsat pornit): Dm7 împins din numărătoare (0:4,62) și Cmaj7 împins peste bara 3 (2:4,73), care înainte se pierdea, primesc acum verdictul pe acordul lor; sesiunea a intrat în `harness/sessions/virtual-timed-anticipation.json`, iar cu regula veche reluarea ei dă exact aceste 2 ca `changed`. Sesiunea reală a lui Edi și cea virtuală se reiau neschimbate. **211 teste.** De știut: sub ~100 bpm, jumătatea de timp trece de debounce, deci un acord anticipat poate primi verdictul înainte ca ecranul să arate acordul lui. **Următorul pas**: demo mode, după aprobarea designului.
- **2026-09-19 (închiderea sesiunii)** — Împinse pe GitHub și ultimele 4 commit-uri (anticiparea); `main` = `origin/main`. **211 teste**, față de 179 la începutul sesiunii. Din P0 sunt făcute execuția pe fraze și harness-ul (cu DoD-ul îndeplinit pe sesiunea reală a lui Edi); `src/theory/` n-a fost atins în toată sesiunea. **De confirmat de Edi**: regula lui a fost „de la «și»-ul lui 4"; am generalizat-o fără să întreb la orice schimbare de acord (și la mijlocul măsurii: G7 pe 3 de la „și"-ul lui 2), la 3/4 („și"-ul lui 3), la numărătoare (primul acord anticipabil) și la sfârșitul unei treceri fără loop (ultimul acord își păstrează voicing-ul). Raportul de predare pentru Cowork e în `docs/session-log/2026-09-19-2258.md`. Porturile 3000 și 8787 sunt ale lui Edi (`start.cmd`), niciun proces al meu nu mai rulează. **Următorul pas**: designul pentru demo mode, propus lui Edi înainte de cod.
- **2026-09-20 (demo mode)** — Design propus și aprobat de Edi înainte de cod, cu patru decizii ale lui: intrarea de pe ecran e **armare + `Enter`** *și* tastatura QWERTY ca pian; piesa demo va fi un **studiu original** al lui, nu o melodie din domeniul public; pagina publicată **pornește în demo**, cu Web MIDI la buton; **`Ask Claude` rămâne live** pe site, cu URL-ul Worker-ului publicat completat implicit. Ideea centrală, acceptată: **demo mode nu e un mod** — clapa desenată e o intrare MIDI virtuală, synth-ul o ieșire în forma unui port MIDI out, iar `mergeAccess` le ține lângă porturile reale, deci captura, banda, statisticile, anticiparea și harness-ul merg neschimbate, fără nicio ramură „dacă e demo". **Implementat**: `audio/context.js` (un singur `AudioContext` pentru metronom și synth, cu conversiile de ceas pure și testate — două contexte ar aluneca, adică aranjamentul s-ar auzi ieșind de pe click), `audio/synth.js`, `midi/virtual.js` (`mergeAccess`, porturi gata făcute ca ieșiri, `idPrefix`), `ui/onscreen.js`, plus legătura din `app.js`, `index.html` și `styles.css`. **Trei lucruri găsite pe drum**: (1) mouse-ul ține o notă, deci un click care ar suna ar da debounce-ului de 300 ms câte un voicing de două note la fiecare click — de aici armarea; (2) clapa desenată nu are corzi: prima probă în browser a dat analiza corectă și **0 oscilatoare**, pentru că ce cânți e intrare, iar synth-ul e ieșire, deci am adăugat ecoul direct spre synth; (3) `type to play` nu pornea singur, dintr-o stare inițială „fără hardware" care nu declanșa niciodată schimbarea. **Cerința fermă 2** (verdictul spune pentru ce acord a fost dat) e făcută: `renderAnalysis` scrie simbolul judecat și marchează anticiparea, iar `isAnticipated` stă **în afara benzii** — un câmp nou în `target` ar fi făcut ca fiecare sesiune salvată să raporteze o schimbare la reluare, pentru o etichetă de prezentare. Odată cu ea, schimbarea de slot făcută de metronom nu mai golește panoul: la anticipare verdictul rămânea pe ecran o clipire (măsurat la 60 bpm în browser). **Cerința fermă 1**: `LIVE_REHARM` doar pe `localhost` sau cu `?reharm=live`; în rest se servește `demo/reharm.json`, cu model, versiune de prompt și dată lângă el, iar fără fișier butonul nu apelează nimic — verificat de pe `http://[::1]:3000`, unde nu pleacă niciun apel. **Verificat în browser** (port virtual): 4 clicuri → E7 „Close position" cu gradele, synth-ul sună (8 oscilatoare pe 4 note), „Play suggestion" iese pe synth, tastele A/D/G țin un acord real, octava se mută cu Z/X, iar o anticipare la 60 bpm primește „Cmaj7, anticipated" și supraviețuiește barei de măsură. **228 de teste** (de la 211); cele trei sesiuni înregistrate, inclusiv cea reală de 11 minute a lui Edi, se reiau 100% la fel. **Rămas**: studiul de 16 măsuri al lui Edi în `demo/piece.json` și un reharm local salvat cu `voicingLab.saveReharm()` în `demo/reharm.json` (format în `demo/README.md`); apoi publicarea. **Următorul pas**: piesa demo de la Edi, apoi publicarea.
- **2026-09-20 (piesa demo iese de pe drumul critic)** — Edi a întrebat de ce mai facem demo-ul de reharm, dacă reharm-ul e scos din produs. Avea dreptate pe accent: `PRODUCT.md` chiar păstrează reharm-ul „în demo-ul public, ca dovadă tehnică", dar **DoD-ul P0 nu-l cere** — scrie doar „demo-ul public merge fără clapă"; „apasă reharmonize pe piesa demo" e în `PHASES.md`, faza veche, iar unde se contrazic câștigă `PRODUCT.md`. Îi descrisesem studiul de 16 măsuri ca și cum rostul lui ar fi să hrănească reharm-ul (cârlige pentru tritone sub, Coltrane), ceea ce contrazice poziționarea produsului. **Decizie Edi**: fără `demo/reharm.json`, **tab-ul Reharm nu apare** pe site-ul publicat; demo-ul arată analiza, drill-ul, progresiile și clapa de ecran, iar dovada arhitecturii hibride rămâne în README. Implementat cu un `HEAD` la fiecare încărcare, deci publicarea răspunsului mai târziu e un fișier, nu o schimbare de cod — verificat în ambele sensuri în browser (fără fișier: „Drill, Progression"; cu un stub: și „Reharm"). Pe `localhost` tab-ul e mereu vizibil, deci sesiunea lui Edi nu se schimbă. **Publicarea nu mai așteaptă nimic de la el**; dacă vrea totuși exemplul salvat, drumul ieftin e o melodie originală scurtă peste o progresie din bibliotecă. **Următorul pas**: publicarea — istoric verificat pentru secrete, licență, README, apoi pașii din contul lui Edi (repo public, Pages, deploy de Worker).
- **2026-09-20 (pregătit de publicare)** — Verificare de secrete pe tot istoricul, înainte de orice: singurele potriviri sunt cheia falsă dintr-un test (`sk-test-not-a-real-key`) și numele variabilei din `worker/check-key.mjs`; `.dev.vars` n-a fost niciodată urmărit, doar `.example` cu valoarea goală, iar `.gitignore` acoperă și variantele pe medii. **Risc ridicat înainte de decizie**: limita Worker-ului e 20 de cereri pe minut pe IP, adică ~12 $/oră dacă cineva o scriptează, chiar și numai pe `explain`. **Decizii Edi**: (1) **licență niciuna**, toate drepturile rezervate, ca să rămână libere opțiunile din P2 — o licență se poate adăuga oricând, nu se poate retrage; `package.json` rămâne `UNLICENSED`, README-ul o spune explicit; (2) **Worker-ul nu se publică**: `PUBLISHED_PROXY_URL` rămâne gol, site-ul publicat nu face niciun apel AI și nu poate costa nimic. Consecința: butonul „Ask Claude" ar fi rămas mort pe site, așa că primește aceeași regulă ca tab-ul Reharm — apare doar când Settings are un proxy, și reapare pe loc dacă cineva lipește URL-ul Worker-ului lui (verificat în browser: pe o gazdă non-localhost rămân „Drill, Progression, Play suggestion, Next"; după ce pui un proxy, „Ask" revine; pe `localhost` totul e neschimbat). **README rescris** în engleză pentru publicare: poziționarea, tabelul cu trei exemple reale de analiză (generate din cod, nu din memorie), diagrama mermaid a lanțului hibrid, „Why not just ask the LLM", cifrele măsurate din `eval/reports/` (0,90 bas față de 0,84 și 4,67 tehnici față de 3,67, trei rulări din fiecare, zero clash-uri), secțiunea despre harness cu sesiunea reală de 11 minute, cum se rulează local, testele, structura și licența. Lipsește doar GIF-ul (comentariu HTML invizibil în README). **Observat, netratat**: `isQuartal` acceptă o 3M plus două 4A (C3 E3 Bb3 E4 iese „Quartal"), pentru că regula cere doar „adiacente de 4P/4A, cu o 3M tolerată de la 4 note". Regula e cea din spec, dar la „So What" terța e sus și cvartele sunt perfecte; dacă vrei, se strânge — e decizie de teorie, deci a lui Edi. **Următorul pas**: repo public + Pages, din contul lui Edi.
- **2026-09-20 (regula quartal strânsă)** — Observația de mai devreme a devenit cerere: `isQuartal` accepta orice adiacente de 4P/4A cu o 3M oriunde, deci `F3 B3 F4 B4` (trei tritoane) ieșea „Quartal". Am pus pe masă ce clasifică azi codul, cu intervalele și cu ce ar deveni fiecare dacă se strânge; **decizie Edi**: cel mult **o 4A** în tot voicing-ul și cel mult **o 3M, numai ca interval de sus**, de la 4 note în sus. Motive, scrise și în `docs/spec-analyzer.md`: două 4A adiacente fac exact o octavă, deci nota extremă e dublată — formă simetrică, nu voicing quartal; terța e capacul din „So What", iar mai jos drop-urile numesc voicing-ul mai bine (`D3 G3 B3 E4` chiar e drop 2 din `G3 B3 D4 E4`). Test întâi, ca la orice atingere a analizorului: testul nou a picat exact pe cazul din sesiune, apoi regula l-a făcut verde. **229 de teste.** **Harness-ul a raportat o diferență voită** și am cerut aprobarea înainte de `--update`, cum cere convenția: un singur verdict schimbat, `D3 G3 B3 E4` pe Em7, `quartal` → `drop-2`, în sesiunea virtuală; sesiunea reală a lui Edi rămâne 93 din 93 neatinsă. **De știut pentru viitor**: `--update` rescrie fișierul din reluare, așa că pe lângă verdictul schimbat au mai apărut două `startedAt` mutate cu 0,1 ms (aceleași verdicte și poziții) — ceasul virtual cade cu o zecime de milisecundă altfel decât cel live, iar toleranța de împerechere e ±1 ms. Nu e derivă.
- **2026-09-20 (redenumire: Voicing Lab → SideKeys)** — Edi a găsit `voicinglab.com`: **VoicingLab Ltd**, companie britanică activă (nr. 17146348, înființată 9 aprilie 2026, deci cu cinci luni înaintea primului nostru commit), cu produs plătit pentru exact aceiași pianiști — 10.674 de voicings, 25 de standarde, practică pe MIDI, conturi de profesor. Numele nostru nu era nici primul, nici apărabil. Am căutat înlocuitor cu patru verificări pe fiecare candidat: domeniul direct la registre (RDAP), urma pe web, registrul de companii britanic (cu „voicinglab" ca test de control, care scoate corect VOICINGLAB LTD) și familia de produse în care ar ateriza. **Ce am învățat pe drum și a tăiat opțiuni**: familia `*Lab` e ocupată (VoicingLab, JazzPianoLab); familia `chord*` e aglomerată (Chordify, Chordana, Chordu, Chorda — de aceea a căzut și **ChorDude**, la două litere de ChordU, propus de Edi și retras după ce i-am arătat coliziunea); „comping" înseamnă în engleza britanică *participare la concursuri cu premii*, e în Cambridge Dictionary și are companii pe numele ăsta, deci **Comping Lab** ar fi adus o ambiguitate nouă; numele scurte sunt parcate de investitori pe toate TLD-urile, deci filtrul de disponibilitate selecta automat variantele urâte. Au mai căzut la verificare Smoothkeys (site de muzică live + artist pe Apple Music), Bigears, Keyside, Touchwise, Bothhands (companii UK), Keypenguin (canale YouTube). **Decizie Edi: SideKeys** — *sidekick* + *keys*, adică exact ideea din ChorDude (un tovarăș care comentează cum cânți) fără să fixeze tonul la „dude" și fără familia `chord*`. `sidekeys.app` liber; `.com` e luat. **Redenumirea în repo**: măturare peste 26 de fișiere urmărite, cu trei excluderi deliberate — `docs/JOURNAL.md` (intrările vechi rămân cum s-au scris; un jurnal e consemnare, nu documentație), `harness/sessions/*.json` (poartă `"app": "voicing-lab"` fiindcă sub numele ăla au fost înregistrate; nimic nu citește câmpul) și `docs/session-log/`. **`src/storage.js`, scris test-întâi**: cheile devin `sidekeys.settings|pieces|stats`, iar ce era salvat sub `voicing-lab.*` se mută singur la prima citire și nu suprascrie niciodată o valoare nouă existentă. Fiecare dintre cele trei module are și un test de cablare, nu doar helper-ul, ca o cablare uitată să pice. **Verificat în browser**, nu doar în teste: cu cele trei chei vechi semănate, după reîncărcare rămân doar `sidekeys.*`, setările păstrează `debounceMs: 450` și `channel: 5`, piesa salvată apare în listă, iar statistica cumulativă arată „All sessions: 77 chords, 40 clean (52%)". **237 de teste**; harness-ul reia toate cele trei sesiuni neschimbat. **De făcut de Edi**: redenumirea repo-ului pe GitHub (link-ul Pages devine `editnss.github.io/SideKeys`), căutare de mărci la EUIPO și UK IPO înainte de a anunța numele, și cumpărarea domeniului. **Următorul pas**: după redenumirea repo-ului, verificat demo-ul pe noul link, apoi GIF și video.
- **2026-09-20 (numele nou e viu)** — Edi a verificat „SideKeys" în TMview: **7 mărci, toate clasa 9, toate moarte** — Siemens le-a depus internațional prin 2003 (probabil pentru tastele laterale de telefon) și le-a lăsat să expire, plus una americană din 1997. Nicio marcă vie, nicăieri. **Ce am adăugat la citire**: TMview caută șirul, nu asemănarea, iar vecinul „Sidekick" are 1.430 de mărci, dintre care 540 înregistrate și 105 depuse, multe în clasa 9 — fonetic și conceptual e aproape, deci **exact acolo** merită o opinie profesionistă, dar abia la P2, când numele are valoare de apărat. **Decizie Edi**: folosim numele acum, înregistrarea vine ulterior. Repo redenumit în `EdiTnss/SideKeys`; remote-ul local actualizat; vechiul link Pages dă 404, cel nou merge. **Verificat pe link-ul publicat**: titlul și h1 sunt SideKeys, tab-urile sunt Drill și Progression (fără Reharm, fără „Ask Claude"), patru clape armate și cântate dau verdict corect (G B D F pe Ebmaj7 → „Wrong note: B", rootless, cu gradele), synth-ul sună, porturile MIDI virtuale se prezintă ca SideKeys. Deployment-ul Pages și workflow-ul de teste sunt verzi pe commit-ul redenumirii. **Următorul pas**: GIF-ul și videoul pentru README, apoi prototipul de microfon.
- **2026-09-20 (manifestul demo-ului)** — Două 404-uri intenționate (`demo/piece.json` și `demo/reharm.json`) apăreau în consola fiecărui vizitator, pentru că aplicația întreba direct de fișiere ca să afle dacă există. Acum întreabă `demo/manifest.json`, care e mereu publicat și le numește, sau are `null`. O singură cerere pe încărcare, consolă curată, iar publicarea fișierelor rămâne o schimbare de date: le adaugi, le treci în manifest, aplicația le ia la următoarea încărcare. `test/demo.test.js` păzește contractul — cheile sunt exact `piece` și `reharm`, fiecare e `null` sau un nume de fișier, iar un nume trecut acolo trebuie să fie un fișier chiar commis, ca manifestul să nu poată promite ce nu există. **Verificat în browser, în ambele sensuri**: cu manifestul gol, zero erori în consolă și singura cerere spre `demo/` e manifestul cu 200; cu un `reharm.json` de probă trecut în manifest, tab-ul Reharm apare pe o gazdă non-localhost fără nicio schimbare de cod. **239 de teste.**
